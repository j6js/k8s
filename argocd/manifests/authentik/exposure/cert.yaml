apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: authentik-tls
  namespace: authentik
spec:
  secretName: authentik-tls
  dnsNames:
    - auth.j6js.com
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
