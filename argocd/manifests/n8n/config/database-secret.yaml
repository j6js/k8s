apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: n8n-db-secrets
  namespace: n8n
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: n8n-db-secrets
    creationPolicy: Owner
    template:
      type: Opaque
      engineVersion: v2
      data:
        username: "{{ .username }}"
        password: "{{ .password }}"
  data:
    - secretKey: username
      remoteRef:
        key: cnpg/role/n8n
        property: username
    - secretKey: password
      remoteRef:
        key: cnpg/role/n8n
        property: password
