apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: vaultwarden
  namespace: default
spec:
  interval: 1h
  chart:
    spec:
      chart: vaultwarden
      version: "0.36.4"
      sourceRef:
        kind: HelmRepository
        name: vaultwarden
        namespace: default
      interval: 1h
