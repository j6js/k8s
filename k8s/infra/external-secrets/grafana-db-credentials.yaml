apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: grafana-db-credentials
  namespace: default
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: grafana-db-credentials
    template:
      type: Opaque
      data:
        POSTGRES_HOST: "{{ .host }}"
        POSTGRES_PORT: "{{ .port }}"
        POSTGRES_DB: "{{ .dbname }}"
        POSTGRES_USER: "{{ .username }}"
        POSTGRES_PASSWORD: "{{ .password }}"
  data:
    - secretKey: username
      remoteRef:
        key: secret/data/cnpg/grafana
        property: username
    - secretKey: password
      remoteRef:
        key: secret/data/cnpg/grafana
        property: password
    - secretKey: host
      remoteRef:
        key: secret/data/cnpg/grafana
        property: host
    - secretKey: port
      remoteRef:
        key: secret/data/cnpg/grafana
        property: port
    - secretKey: dbname
      remoteRef:
        key: secret/data/cnpg/grafana
        property: dbname
