rules:
  - name: nlb-backend-traffic
    description: "Allow health checks and traffic from NLB to backend services"
    direction: ingress
    protocol: tcp
    ports: [30080, 30443]
    source: internet
    destination: ""
    for_roles: [cp, sh, wk]
